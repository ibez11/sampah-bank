var isStep1Enabled = true;
var isStep2Enabled = false;
var isStep3Enabled = false;

$(document).ready(function() {
  var navList = $('div.setup-panel div');
	var navListItems = $('div.setup-panel div a');
	var allWells = $('.setup-content');

  allWells.hide();
});
